---
# Source: harbor/templates/registry/registry-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: "kccr-harbor-registry"
  labels:
    heritage: Helm
    release: kccr
    chart: harbor
    app: "harbor"
spec:
  ports:
    - name: http-registry
      port: 5000

    - name: http-controller
      port: 8080
  selector:
    release: kccr
    app: "harbor"
    component: registry
